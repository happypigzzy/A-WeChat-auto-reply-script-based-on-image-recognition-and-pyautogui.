# A-WeChat-auto-reply-script-based-on-image-recognition-and-pyautogui.
This program uses OpenCV to detect whether there are messages and controls the PC to reply to messages through PyAutoGUI, thereby simulating user responses to bypass Tencent's detection. It can read the clipboard to automatically reply (later, a feature to connect to an API for AI automatic replies will be added).
